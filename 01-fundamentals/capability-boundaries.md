# Capability Boundaries

A skill should have one coherent responsibility.

Avoid skills like `software-engineer-everything`. Prefer `code-reviewer`, `dependency-auditor`, `test-writer`, or `api-contract-reviewer`.
